{
  "name": "mcp-gateway-monorepo",
  "private": true,
  "version": "0.6.0",
  "description": "MSPStack Gateway — self-hosted MCP manager: one endpoint federating many MCP servers with OAuth, roles, secret storage, and tool toggles",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm run dev --workspace packages/gateway"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "author": "Eugene Samotija (selic) <es@defency.net> (https://defency.net)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mspstack/mcp-gateway.git"
  },
  "homepage": "https://github.com/mspstack/mcp-gateway#readme",
  "bugs": {
    "url": "https://github.com/mspstack/mcp-gateway/issues"
  }
}
